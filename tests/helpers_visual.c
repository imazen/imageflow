#include "helpers_visual.h"



bool visual_compare(flow_c * c, struct flow_bitmap_bgra * bitmap, const char * name){
    //compute checksum of bitmap (two checksums, actually - one for configuration, another for bitmap bytes)
    //load checksums file statically
    //If checksum matches, return true.

    //If it fails
    //Export PNG to tests/visuals
    //Try to download "old" png from S3 using the checksums as an address.
    //Diff the two, generate a third PNG. Also
    //Dump to HTML



    return true;
}
